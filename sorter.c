#include<stdio.h>
#include "code.h"

int main(int argc, char *argv[])
{
    FILE original = fopen("mycokerewards.csv","r");
    FILE updated = fopen("sorted.csv","w");
    char[20] codetype;
    char[15] cokecode;
    struct code *temp = malloc (sizeof(code));


    while (1)
    {
        
    }

}
